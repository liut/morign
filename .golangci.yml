version: "2"

linters:
  default: standard
  exclusions:
    rules:
      # AGENTS.md 规定勿动 codegen 生成的 _gen.go 文件
      - path: '_gen\.go$'
        linters:
          - staticcheck
      # Yoda 条件为历史风格，集中在 pkg/settings，不强制修复
      - linters:
          - staticcheck
        text: 'ST1017:'